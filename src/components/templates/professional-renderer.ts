import { categoryToProfessionalTemplate, isSiteTemplate, type PreviewData, type ProfessionalTemplateName } from './types'
import { QUIRKY_TEMPLATE_CSS } from './quirky-template-styles'

export type PublicSiteData = Partial<PreviewData> & { slug: string; business_name: string }
export interface SiteRenderOptions { siteUrl?: string; apiBaseUrl?: string; mode?: 'live' | 'preview' | 'demo' }

export function escapeHtml(value: unknown): string {
  return String(value ?? '').replace(/[&<>"']/g, character => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[character]!))
}
export function safeWebUrl(value: unknown): string {
  if (typeof value !== 'string') return ''
  try {
    const url = new URL(value)
    if (!['http:', 'https:'].includes(url.protocol) || url.username || url.password) return ''
    // Never render credential-bearing Google media URLs in a public document.
    if (url.searchParams.has('key') || url.searchParams.has('access_token')) return ''
    return url.href
  } catch { return '' }
}
function publicEmail(data: PublicSiteData) {
  const email = data.contact_email?.trim() || ''
  return /^[^\s<>@]+@[^\s<>@]+\.[^\s<>@]+$/.test(email) ? email : ''
}
function phoneLink(phone: unknown) {
  const normalized = String(phone || '').replace(/[^+\d]/g, '')
  return normalized.replace(/\D/g, '').length >= 5 ? `tel:${normalized}` : ''
}
function servicesFor(data: PublicSiteData) {
  return (Array.isArray(data.services) ? data.services : []).filter(service => service && typeof service.name === 'string' && service.name.trim()).slice(0, 30)
}
function hoursFor(data: PublicSiteData) {
  const days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday']
  return days.flatMap(day => {
    const entry = Object.entries(data.hours || {}).find(([key]) => key.toLowerCase() === day.toLowerCase() || key.toLowerCase() === day.slice(0, 3).toLowerCase())
    return entry && typeof entry[1] === 'string' && entry[1].trim() ? [[day, entry[1]]] : []
  })
}
function locationFor(data: PublicSiteData) { return [data.city, data.state].filter(Boolean).join(', ') }
export function resolveSiteUrl(data: PublicSiteData, supplied?: string): string {
  const explicit = safeWebUrl(supplied)
  if (explicit) return explicit.replace(/\/$/, '')
  if (data.custom_domain && data.domain_status === 'active') {
    const domain = safeWebUrl(`https://${data.custom_domain.replace(/^https?:\/\//, '').replace(/\/$/, '')}`)
    if (domain) return domain.replace(/\/$/, '')
  }
  const slug = /^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?$/.test(data.slug) ? data.slug : 'preview'
  return `https://${slug}.autolocal.ai`
}
export function isProfessionalTemplate(template: string): template is ProfessionalTemplateName {
  return ['summit', 'atelier', 'ledger'].includes(template)
}

export const PROFESSIONAL_CSS = String.raw`
.al-site{--al-ink:#102e29;--al-paper:#f5f4ee;--al-muted:#52645e;--al-line:#ccd2ca;--al-accent:#d9e844;--al-panel:#e6e9e1;background:var(--al-paper);color:var(--al-ink);font-family:Arial,Helvetica,sans-serif;font-size:16px;line-height:1.6;overflow:clip;color-scheme:light;isolation:isolate}
.al-site *{box-sizing:border-box}.al-site h1,.al-site h2,.al-site h3,.al-site p,.al-site figure{margin:0}.al-site a{color:inherit;text-decoration:none}.al-site button,.al-site input,.al-site textarea,.al-site select{font:inherit}.al-site a,.al-site button{-webkit-tap-highlight-color:transparent}.al-site a:focus-visible,.al-site button:focus-visible,.al-site input:focus-visible,.al-site select:focus-visible,.al-site textarea:focus-visible,.al-site summary:focus-visible{outline:3px solid #3270bb;outline-offset:5px}.al-site img{display:block;width:100%;height:100%;object-fit:cover}.al-site [id]{scroll-margin-top:100px}.al-site .al-shell{width:min(1280px,calc(100% - 96px));margin:auto}.al-site .al-skip{position:absolute;top:8px;left:8px;z-index:1000;background:var(--al-ink);color:var(--al-paper);padding:12px;transform:translateY(-180%)}.al-site .al-skip:focus{transform:none}.al-site .al-demo{padding:9px 24px;text-align:center;background:var(--al-panel);font-size:11px;letter-spacing:.16em;text-transform:uppercase}
.al-site .al-header{min-height:98px;display:flex;align-items:center;justify-content:space-between;gap:28px;border-bottom:1px solid var(--al-line)}.al-site .al-brand{display:flex;gap:13px;align-items:center;font-size:19px;font-weight:750;line-height:1.15;letter-spacing:-.03em;max-width:320px}.al-site .al-brand img{width:40px;height:40px;object-fit:contain}.al-site .al-brand-mark{display:grid;place-items:center;width:37px;height:37px;flex-shrink:0;border:1px solid currentColor;font-size:21px}.al-site .al-nav{display:flex;align-items:center;gap:30px;font-size:13px}.al-site .al-nav a:hover{text-decoration:underline;text-underline-offset:5px}.al-site .al-button{display:inline-flex;justify-content:center;align-items:center;gap:34px;min-height:50px;padding:13px 23px;background:var(--al-ink);color:var(--al-paper);border:1px solid transparent;font-size:13px;font-weight:650;line-height:1.4;cursor:pointer;text-align:center;transition:transform .18s ease,opacity .18s ease}.al-site .al-button:hover{transform:translateY(-2px)}.al-site .al-button:disabled{opacity:.6;cursor:wait}.al-site .al-button-accent{background:var(--al-accent);color:#142a20}.al-site .al-arrow{font-size:21px;line-height:1}.al-site .al-text-link{display:inline-flex;gap:20px;align-items:center;border-bottom:1px solid var(--al-line);padding:8px 0;font-size:14px}
.al-site .al-kicker{font-size:10px;text-transform:uppercase;letter-spacing:.21em;font-weight:650;line-height:1.7}.al-site .al-kicker:before{content:'';display:inline-block;width:28px;height:1px;vertical-align:middle;background:currentColor;margin-right:14px}.al-site .al-hero{position:relative;display:grid;grid-template-columns:1.1fr 1fr;gap:64px;padding-top:72px;padding-bottom:76px;align-items:center}.al-site .al-hero h1{font-size:clamp(44px,6.5vw,91px);line-height:.98;letter-spacing:-.065em;font-weight:780;text-wrap:balance;overflow-wrap:anywhere;max-width:850px}.al-site .al-hero .al-kicker{margin-bottom:30px}.al-site .al-intro{font-size:18px;line-height:1.6;color:var(--al-muted);max-width:520px;margin-top:28px}.al-site .al-actions{display:flex;align-items:center;gap:28px;flex-wrap:wrap;margin-top:34px}.al-site .al-hero-media{height:520px;position:relative;min-width:0}.al-site .al-image{height:100%;overflow:hidden;position:relative}.al-site .al-image figcaption{font-size:10px;letter-spacing:.03em;line-height:1.3;background:var(--al-paper);padding:8px 0;color:var(--al-muted)}.al-site .al-image:has(figcaption) img{height:calc(100% - 30px)}
.al-site .al-art{height:100%;background:var(--al-ink);overflow:hidden;position:relative;display:flex;flex-direction:column;justify-content:space-between;padding:36px;color:var(--al-paper)}.al-site .al-art:before{content:'';position:absolute;width:85%;height:100%;border:1px solid #ffffff33;transform:rotate(-32deg);left:40%;top:20%}.al-site .al-art:after{content:'';position:absolute;width:48%;height:85%;border:1px solid #ffffff33;left:12%;top:35%;transform:rotate(-32deg)}.al-site .al-art-mark{font-size:clamp(90px,17vw,225px);font-weight:750;line-height:1;letter-spacing:-.08em;z-index:1;color:var(--al-accent)}.al-site .al-art-caption{z-index:1;font-size:12px;max-width:220px;letter-spacing:.07em}.al-site .al-art-dot{display:block;width:17px;height:17px;background:var(--al-accent)}
.al-site .al-service-strip{background:var(--al-ink);color:var(--al-paper)}.al-site .al-strip-inner{display:flex;flex-wrap:wrap}.al-site .al-strip-inner a{padding:22px 28px 22px 0;flex:1;min-width:180px;display:flex;align-items:center;gap:22px;font-size:17px;border-right:1px solid #ffffff26;margin:15px 30px 15px 0}.al-site .al-strip-inner a:last-child{border:0;margin-right:0}.al-site .al-number{font-size:11px;font-weight:500;letter-spacing:.05em;opacity:.6}.al-site .al-strip-inner .al-number{color:var(--al-accent);opacity:1}
.al-site .al-section{padding-top:94px;padding-bottom:94px}.al-site .al-section-head{display:flex;justify-content:space-between;align-items:flex-start;gap:45px;margin-bottom:44px}.al-site h2{font-size:clamp(32px,4vw,53px);line-height:1.1;letter-spacing:-.045em;font-weight:650;text-wrap:balance}.al-site .al-section-head .al-kicker{padding-top:8px;flex-shrink:0}.al-site .al-services{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:0}.al-site .al-service{border-top:1px solid var(--al-line);padding:30px 30px 32px 0;margin-right:32px;position:relative}.al-site .al-service h3{font-size:22px;line-height:1.3;letter-spacing:-.02em;margin:25px 0 13px}.al-site .al-service p{font-size:14px;color:var(--al-muted)}.al-site .al-service .al-price{color:var(--al-ink);margin-top:13px;font-weight:650}.al-site .al-service a{display:inline-flex;gap:25px;align-items:center;font-size:12px;margin-top:28px}.al-site .al-service:last-child{margin-right:0}.al-site .al-about-band{background:var(--al-panel)}.al-site .al-facts{display:grid;grid-template-columns:1.2fr 1fr;gap:80px;align-items:start}.al-site .al-facts h2{margin-top:20px}.al-site .al-fact-list{display:grid;grid-template-columns:1fr 1fr;gap:30px;font-size:15px}.al-site .al-fact-list dt{font-size:10px;letter-spacing:.13em;text-transform:uppercase;color:var(--al-muted);margin-bottom:9px}.al-site .al-fact-list dd{margin:0}.al-site .al-fact-list a{text-decoration:underline;text-underline-offset:4px;overflow-wrap:anywhere}.al-site .al-hours{display:grid;gap:8px;margin-top:25px}.al-site .al-hours div{display:flex;justify-content:space-between;gap:22px;border-bottom:1px solid var(--al-line);padding-bottom:8px;font-size:13px}.al-site .al-hours dd,.al-site .al-hours dt{margin:0}.al-site .al-gallery{display:grid;grid-template-columns:1.2fr 1fr 1fr;gap:20px}.al-site .al-gallery figure{height:300px}.al-site .al-review-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:35px}.al-site blockquote{margin:0;padding:30px 0;border-top:1px solid var(--al-line);font-size:18px}.al-site blockquote cite{display:block;font-size:12px;margin-top:25px;font-style:normal;color:var(--al-muted)}
.al-site .al-faq-grid{display:grid;grid-template-columns:.8fr 1.2fr;gap:90px}.al-site .al-faq-grid h2{margin-top:22px}.al-site .al-faq details{padding:22px 0;border-top:1px solid var(--al-line)}.al-site .al-faq details:last-child{border-bottom:1px solid var(--al-line)}.al-site .al-faq summary{list-style:none;cursor:pointer;font-size:17px;display:flex;justify-content:space-between;gap:30px}.al-site .al-faq summary::-webkit-details-marker{display:none}.al-site .al-faq summary:after{content:'+';font-size:23px}.al-site .al-faq details[open] summary:after{content:'−'}.al-site .al-faq p{color:var(--al-muted);font-size:14px;margin-top:16px;max-width:570px}
.al-site .al-contact{border-top:1px solid var(--al-line);display:grid;grid-template-columns:.85fr 1.15fr;gap:90px}.al-site .al-contact h2{margin-top:22px}.al-site .al-contact-intro>p{margin-top:24px;color:var(--al-muted);max-width:370px}.al-site .al-contact-links{display:grid;justify-items:start;gap:14px;margin-top:32px}.al-site .al-form{display:grid;grid-template-columns:1fr 1fr;gap:23px}.al-site .al-form label{font-size:12px;display:block}.al-site .al-form input,.al-site .al-form textarea,.al-site .al-form select{width:100%;display:block;margin-top:8px;min-height:47px;padding:11px 13px;color:var(--al-ink);background:transparent;border:1px solid var(--al-line);border-radius:0;line-height:1.4}.al-site .al-form textarea{min-height:115px;resize:vertical}.al-site .al-form .al-full{grid-column:1/-1}.al-site .al-form .al-form-note{font-size:12px;color:var(--al-muted);margin:0}.al-site .al-form .al-form-status{font-size:14px;min-height:25px;margin:0;outline:none;overflow-wrap:anywhere}.al-site .al-form .al-form-actions{display:flex;align-items:center;gap:22px}.al-site .al-honeypot{position:absolute;left:-10000px;width:1px;height:1px;overflow:hidden}.al-site .al-footer{background:var(--al-ink);color:var(--al-paper);padding:42px 0}.al-site .al-footer-inner{display:flex;justify-content:space-between;gap:25px;align-items:center}.al-site .al-footer p,.al-site .al-footer a{font-size:11px;opacity:.8}.al-site .al-footer nav{display:flex;gap:20px}.al-site .al-mobile-contact{display:none}
.al-site.al-atelier{--al-ink:#482535;--al-paper:#fbf7f1;--al-muted:#77616a;--al-line:#dbcdc8;--al-accent:#eddbd0;--al-panel:#eee2db}.al-atelier .al-brand{font-family:Georgia,'Times New Roman',serif;font-size:26px;font-weight:400}.al-atelier .al-brand-mark{border-radius:50%;font-weight:400;font-size:22px}.al-atelier .al-button{border-radius:50px;font-weight:500;padding-left:27px;padding-right:27px}.al-atelier .al-button-accent{background:var(--al-ink);color:var(--al-paper)}.al-atelier .al-hero{grid-template-columns:1.1fr 1fr;gap:85px;padding-top:68px;padding-bottom:100px}.al-atelier .al-hero h1{font-family:Georgia,'Times New Roman',serif;font-weight:400;font-size:clamp(46px,6.4vw,86px);line-height:1.03;letter-spacing:-.055em}.al-atelier .al-intro{font-family:Georgia,'Times New Roman',serif;max-width:400px;font-size:18px}.al-atelier .al-hero-media{height:570px;padding-left:20px}.al-atelier .al-hero-media:before{content:'';background:var(--al-panel);position:absolute;width:75%;height:80%;right:-35px;top:75px}.al-atelier .al-hero-media .al-image{border-top-left-radius:180px;border-top-right-radius:180px}.al-atelier .al-art{border-radius:220px 220px 0 0;background:#e7d8cb;color:var(--al-ink);padding-top:95px}.al-atelier .al-art:before{border-color:#48253530;border-radius:50%;width:90%;left:-35%;top:10%;transform:rotate(30deg)}.al-atelier .al-art:after{border-color:#48253530;border-radius:50%;width:90%;left:35%;top:25%;transform:rotate(30deg)}.al-atelier .al-art-mark{font-family:Georgia,'Times New Roman',serif;font-weight:400;color:var(--al-ink)}.al-atelier .al-art-dot{border-radius:50%;background:var(--al-ink)}.al-atelier .al-service-strip{background:var(--al-paper);color:var(--al-ink);border-top:1px solid var(--al-line);border-bottom:1px solid var(--al-line)}.al-atelier .al-strip-inner a{font-family:Georgia,'Times New Roman',serif;border-color:var(--al-line);font-size:22px}.al-atelier .al-strip-inner .al-number{color:var(--al-muted)}.al-atelier h2,.al-atelier .al-service h3{font-family:Georgia,'Times New Roman',serif;font-weight:400;letter-spacing:-.045em}.al-atelier .al-services{grid-template-columns:1fr 1fr;gap:20px 70px}.al-atelier .al-service{display:grid;grid-template-columns:35px 1fr;column-gap:15px;margin:0;padding:26px 0}.al-atelier .al-service h3{font-size:28px;margin:0 0 14px}.al-atelier .al-service p,.al-atelier .al-service a{grid-column:2}.al-atelier .al-service .al-number{padding-top:5px}.al-atelier .al-gallery{grid-template-columns:1fr 1fr 1fr;align-items:center;gap:40px}.al-atelier .al-gallery figure:nth-child(2){height:380px}.al-atelier .al-faq summary{font-family:Georgia,'Times New Roman',serif;font-size:21px}
.al-site.al-ledger{--al-ink:#132e49;--al-paper:#fcfcf8;--al-muted:#52667a;--al-line:#c7d1d9;--al-accent:#ec783f;--al-panel:#eaf1f5}.al-ledger .al-brand{font-family:Georgia,'Times New Roman',serif;font-size:26px;font-weight:400}.al-ledger .al-brand-mark{border:0;border-bottom:2px solid currentColor;font-size:27px}.al-ledger .al-hero{grid-template-columns:1.55fr .85fr;gap:55px;min-height:595px;padding-top:85px;padding-bottom:90px}.al-ledger .al-hero h1{font-family:Georgia,'Times New Roman',serif;font-weight:400;letter-spacing:-.06em;line-height:1.02;font-size:clamp(45px,6.4vw,86px)}.al-ledger .al-hero-media{height:430px;margin-right:-48px}.al-ledger .al-hero .al-intro{font-size:17px;max-width:480px}.al-ledger .al-button-accent{background:var(--al-ink);color:var(--al-paper)}.al-ledger .al-art{background:var(--al-panel);color:var(--al-ink);padding:30px;justify-content:flex-end;border-right:65px solid var(--al-ink)}.al-ledger .al-art:before{width:95%;left:18%;top:35%;transform:rotate(-30deg);border-color:#132e4944;background:linear-gradient(90deg,transparent,#132e4908)}.al-ledger .al-art:after{width:70%;height:65%;left:-5%;top:50%;border-color:#132e4944;transform:rotate(-30deg)}.al-ledger .al-art-mark{font-family:Georgia,'Times New Roman',serif;font-size:100px;font-weight:400;color:var(--al-ink);position:absolute;top:30px;left:27px}.al-ledger .al-art-dot{position:absolute;top:36%;right:22%;width:24px;height:24px}.al-ledger .al-art-caption{max-width:190px;font-size:10px;text-transform:uppercase;letter-spacing:.18em}.al-ledger .al-service-strip{background:var(--al-panel);color:var(--al-ink)}.al-ledger .al-strip-inner .al-number{color:var(--al-ink)}.al-ledger .al-strip-inner a{border-color:var(--al-line);font-size:15px}.al-ledger h2,.al-ledger .al-service h3{font-family:Georgia,'Times New Roman',serif;font-weight:400}.al-ledger .al-services{gap:35px}.al-ledger .al-service{border:0;border-right:1px solid var(--al-line);padding-top:0;margin:0}.al-ledger .al-service:last-child{border-right:0}.al-ledger .al-service .al-number{font-size:12px;border:1px solid var(--al-line);width:36px;height:36px;display:grid;place-items:center;opacity:1}.al-ledger .al-section-head{margin-bottom:55px}.al-ledger .al-contact{border:0}.al-ledger .al-about-band{border-top:1px solid var(--al-line);border-bottom:1px solid var(--al-line);background:var(--al-paper)}
@media(min-width:1440px){.al-site .al-hero h1{font-size:92px}.al-atelier .al-hero h1,.al-ledger .al-hero h1{font-size:91px}}
@media(max-width:1000px){.al-site .al-shell{width:calc(100% - 56px)}.al-site .al-nav{gap:18px}.al-site .al-header>.al-button{display:none}.al-site .al-hero{gap:36px}.al-site .al-hero-media{height:440px}.al-site .al-hero h1{font-size:64px}.al-site .al-facts,.al-site .al-faq-grid,.al-site .al-contact{gap:45px}.al-site .al-services{grid-template-columns:repeat(2,minmax(0,1fr))}.al-ledger .al-hero-media{margin-right:-28px}.al-ledger .al-art{border-right-width:30px}.al-atelier .al-hero-media{padding-left:0}.al-atelier .al-hero-media:before{right:-20px}.al-site .al-gallery figure{height:235px}}
@media(max-width:700px){.al-site{padding-bottom:69px}.al-site .al-shell{width:calc(100% - 40px)}.al-site .al-header{min-height:81px;gap:18px;flex-wrap:wrap;padding:20px 0}.al-site .al-brand{font-size:18px;max-width:75%}.al-site .al-brand-mark{width:28px;height:28px;font-size:17px}.al-site .al-brand img{width:30px;height:30px}.al-site .al-nav{margin-left:auto;font-size:12px;gap:14px}.al-site .al-nav a:nth-child(2){display:none}.al-site .al-demo{font-size:9px;letter-spacing:.09em;padding:8px 16px}.al-site .al-hero{grid-template-columns:1fr;gap:38px;padding-top:43px;padding-bottom:44px}.al-site .al-hero h1{font-size:clamp(43px,11.5vw,67px);max-width:600px;line-height:1.02;letter-spacing:-.06em}.al-site .al-hero .al-kicker{margin-bottom:24px;font-size:9px}.al-site .al-intro{font-size:16px;margin-top:22px}.al-site .al-actions{gap:20px;margin-top:27px}.al-site .al-button{min-height:49px;padding:13px 20px;font-size:12px;gap:25px}.al-site .al-hero-media{height:330px}.al-site .al-art{padding:27px}.al-site .al-art-mark{font-size:145px}.al-site .al-strip-inner{display:block}.al-site .al-strip-inner a{padding:16px 0;margin:0;border:0;border-bottom:1px solid #ffffff26;min-width:0;font-size:18px}.al-site .al-strip-inner .al-arrow{margin-left:auto}.al-site .al-strip-inner a:last-child{border:0}.al-site .al-section{padding-top:58px;padding-bottom:58px}.al-site .al-section-head{display:block;margin-bottom:28px}.al-site .al-section-head .al-kicker{margin-bottom:18px}.al-site h2{font-size:36px}.al-site .al-services{display:block}.al-site .al-service{padding:24px 0;margin:0;border-right:0}.al-site .al-service h3{font-size:24px;margin:14px 0 10px}.al-site .al-service a{margin-top:19px}.al-site .al-facts,.al-site .al-contact,.al-site .al-faq-grid{grid-template-columns:1fr;gap:32px}.al-site .al-fact-list{gap:24px}.al-site .al-faq-grid h2{margin-top:16px}.al-site .al-faq summary{font-size:16px}.al-site .al-form{gap:18px;grid-template-columns:1fr}.al-site .al-form .al-full{grid-column:auto}.al-site .al-form input,.al-site .al-form textarea,.al-site .al-form select{font-size:16px}.al-site .al-form .al-form-actions{display:block}.al-site .al-form .al-form-actions button{width:100%}.al-site .al-gallery{grid-template-columns:1fr 1fr;gap:12px}.al-site .al-gallery figure,.al-atelier .al-gallery figure:nth-child(2){height:200px}.al-site .al-gallery figure:first-child{grid-column:1/-1;height:280px}.al-site .al-review-grid{grid-template-columns:1fr;gap:15px}.al-site .al-footer-inner{display:block}.al-site .al-footer .al-brand{max-width:none;margin-bottom:20px}.al-site .al-footer p{margin-top:20px}.al-site .al-footer nav{margin-top:20px}.al-site .al-mobile-contact{display:flex;position:fixed;bottom:0;left:0;right:0;z-index:30;background:var(--al-paper);border-top:1px solid var(--al-line);padding:10px 20px;gap:10px;box-shadow:0 -4px 20px #00000008}.al-site .al-mobile-contact a{flex:1;min-height:46px;padding:10px 12px}.al-atelier .al-brand,.al-ledger .al-brand{font-size:22px}.al-atelier .al-hero-media{height:410px;padding:0 14px}.al-atelier .al-hero-media:before{right:0;top:55px;width:85%;height:80%}.al-atelier .al-hero-media .al-image{border-top-left-radius:180px;border-top-right-radius:180px}.al-atelier .al-hero{padding-top:45px}.al-atelier .al-hero h1{font-size:56px}.al-atelier .al-service{display:grid;grid-template-columns:30px 1fr;gap:0 12px}.al-atelier .al-service h3{margin-top:0;font-size:27px}.al-atelier .al-strip-inner a{border-color:var(--al-line);font-size:21px}.al-ledger .al-hero{gap:35px;min-height:0}.al-ledger .al-hero-media{height:250px;margin-right:0}.al-ledger .al-art{border-right-width:45px}.al-ledger .al-art-mark{font-size:80px}.al-ledger .al-art-caption{font-size:9px;max-width:150px}.al-ledger .al-service{border-top:1px solid var(--al-line)}.al-ledger .al-strip-inner a{border-color:var(--al-line);font-size:16px}}
@media(prefers-reduced-motion:reduce){.al-site *{scroll-behavior:auto!important;transition:none!important}}
${QUIRKY_TEMPLATE_CSS}
.al-site .al-image{display:flex;flex-direction:column}.al-site .al-image:has(figcaption) img{flex:1;min-height:0;height:0}.al-site .al-image figcaption{flex-shrink:0;padding:9px 10px;font-size:12px;line-height:1.5}.al-site .al-photo-credit{font:12px/1.5 Arial,sans-serif;overflow-wrap:anywhere;color:var(--al-ink);background:var(--al-paper);padding:9px 0}.al-site .al-photo-credit a,.al-site .al-source-credit a{text-decoration:underline;text-underline-offset:3px}.al-site .al-source-credit{font-size:12px;line-height:1.6;margin-top:20px;color:var(--al-muted);overflow-wrap:anywhere}.al-site .al-gallery figure:has(figcaption){height:auto}.al-site .al-gallery figure:has(figcaption) img{height:300px}.al-atelier .al-gallery figure:nth-child(2):has(figcaption) img{height:380px}
@media(max-width:1000px){.al-site .al-gallery figure:has(figcaption) img{height:235px}}
@media(max-width:700px){.al-site .al-hero:has(.al-photo-credit){gap:24px;padding-top:26px}.al-site .al-hero:has(.al-photo-credit) .al-hero-media{order:1;height:260px}.al-site .al-hero:has(.al-photo-credit) .al-hero-copy{order:2}.al-site .al-gallery:has(figcaption){grid-template-columns:1fr}.al-site .al-gallery figure:has(figcaption) img,.al-site .al-gallery figure:first-child:has(figcaption) img{height:250px}}
`;

function factsQuestions(data: PublicSiteData) {
  const supplied = (Array.isArray(data.faq) ? data.faq : []).filter(item => typeof item?.question === 'string' && typeof item?.answer === 'string' && item.question.trim() && item.answer.trim()).slice(0, 12)
  if (supplied.length) return supplied
  const questions: { question: string; answer: string }[] = []
  const services = servicesFor(data)
  if (services.length) questions.push({ question: 'What services are available?', answer: services.map(service => service.name).join(', ') + '.' })
  const areas = (data.service_areas || []).filter(area => typeof area === 'string' && area.trim())
  if (areas.length) questions.push({ question: 'Which areas do you serve?', answer: areas.join(', ') + '.' })
  else if (locationFor(data)) questions.push({ question: 'Where is the business based?', answer: `${data.business_name} is based in ${locationFor(data)}.` })
  questions.push({ question: 'How can I get in touch?', answer: phoneLink(data.phone) ? `Call ${data.phone} or use the inquiry form below.` : 'Use the inquiry form below to share what you need and how to reach you.' })
  return questions
}
export function siteStructuredData(data: PublicSiteData, siteUrl: string) {
  const businessId = `${siteUrl}/#business`
  const services = servicesFor(data)
  return { '@context': 'https://schema.org', '@graph': [
    { '@type': 'LocalBusiness', '@id': businessId, name: data.business_name, url: siteUrl,
      ...(data.description ? { description: data.description } : {}),
      ...(phoneLink(data.phone) ? { telephone: data.phone } : {}),
      ...(publicEmail(data) ? { email: publicEmail(data) } : {}),
      ...(safeWebUrl(data.logo_url) ? { logo: safeWebUrl(data.logo_url) } : {}),
      ...(safeWebUrl(data.hero_image_url) ? { image: safeWebUrl(data.hero_image_url) } : {}),
      ...(data.address && data.show_address !== false ? { address: { '@type': 'PostalAddress', streetAddress: data.address, ...(data.city ? { addressLocality: data.city } : {}), ...(data.state ? { addressRegion: data.state } : {}) } } : {}),
      ...(data.service_areas?.length ? { areaServed: data.service_areas } : {}),
      ...(services.length ? { hasOfferCatalog: { '@type': 'OfferCatalog', name: 'Services', itemListElement: services.map((service, index) => ({ '@type': 'Offer', itemOffered: { '@id': `${siteUrl}/#service-${index + 1}` } })) } } : {}),
    },
    ...services.map((service, index) => ({ '@type': 'Service', '@id': `${siteUrl}/#service-${index + 1}`, name: service.name, ...(service.description ? { description: service.description } : {}), provider: { '@id': businessId } })),
  ] }
}
function brand(data: PublicSiteData) {
  const logo = safeWebUrl(data.logo_url)
  return `<a class="al-brand" href="#main">${logo ? `<img src="${escapeHtml(logo)}" alt="" width="40" height="40">` : `<span class="al-brand-mark" aria-hidden="true">${escapeHtml(data.business_name.trim().charAt(0))}</span>`}<span>${escapeHtml(data.business_name)}</span></a>`
}
function inquiryForm(data: PublicSiteData, options: SiteRenderOptions) {
  const mode = options.mode || (data.demo ? 'demo' : ['active', 'pending_cancel'].includes(data.hosting_status || '') ? 'live' : 'preview')
  const endpoint = `${safeWebUrl(options.apiBaseUrl)?.replace(/\/$/, '') || ''}/api/leads/submit`
  const services = servicesFor(data)
  return `<form class="al-form" data-al-inquiry data-slug="${escapeHtml(data.slug)}" data-mode="${mode}" data-endpoint="${escapeHtml(endpoint)}">
    <label>Name <span aria-hidden="true">*</span><input name="name" autocomplete="name" maxlength="150" required></label>
    <label>Email<input name="email" type="email" autocomplete="email" maxlength="254"></label>
    <label>Phone<input name="phone" type="tel" autocomplete="tel" maxlength="40"></label>
    ${services.length ? `<label>Interested in<select name="service"><option value="">Choose a service (optional)</option>${services.map(service => `<option>${escapeHtml(service.name)}</option>`).join('')}</select></label>` : ''}
    <p class="al-form-note al-full">Please include an email address or phone number so we can reply.</p>
    <label class="al-full">How can we help?<textarea name="message" rows="4" maxlength="2000"></textarea></label>
    <div class="al-honeypot" aria-hidden="true"><label>Leave this empty<input name="website" autocomplete="off" tabindex="-1"></label></div>
    <p class="al-form-note al-full">${mode === 'demo' ? 'Fictional business demo. This form does not send inquiries.' : mode === 'preview' ? 'Private website preview. Inquiries are available after the site is live.' : `Your details are shared with ${escapeHtml(data.business_name)} to respond to this inquiry.`}</p>
    <div class="al-form-actions al-full"><button class="al-button" type="submit">${mode === 'demo' ? 'Try the form' : 'Send inquiry'} <span class="al-arrow" aria-hidden="true">→</span></button></div>
    <p class="al-form-status al-full" data-form-status role="status" aria-live="polite" tabindex="-1"></p>
    <noscript><p class="al-form-note al-full">To send an inquiry through this form, enable JavaScript or use the contact details on this page.</p></noscript>
  </form>`
}
export function renderProfessionalSite(data: PublicSiteData, template?: string, options: SiteRenderOptions = {}) {
  const family = template && isSiteTemplate(template) ? template : categoryToProfessionalTemplate(data.category || '')
  const e = escapeHtml
  const services = servicesFor(data)
  const phone = phoneLink(data.phone)
  const email = publicEmail(data)
  const location = locationFor(data)
  const googlePhotos = (data.google_photos || []).filter(photo => safeWebUrl(photo.url)).slice(0, 10)
  const ownerPhoto = safeWebUrl(data.hero_image_url)
  const photo = ownerPhoto || safeWebUrl(googlePhotos[0]?.url)
  const googlePhotoFor = (url: string) => googlePhotos.find(photo => safeWebUrl(photo.url) === url)
  const gallery = [...new Set([...(data.gallery_images || []).slice(0, 20), ...googlePhotos.map(photo => photo.url)].map(safeWebUrl).filter(url => url && url !== photo))]
  const attributionLinks = (attributions: NonNullable<PreviewData['google_attributions']>) => attributions.map(author => safeWebUrl(author.uri) ? `<a href="${e(safeWebUrl(author.uri))}" target="_blank" rel="noopener noreferrer">${e(author.displayName)}</a>` : e(author.displayName)).join(', ')
  const photoCredit = (url: string) => {
    const source = googlePhotoFor(url)
    return source ? `<figcaption class="al-photo-credit">Google Maps${source.attributions.length ? ` · ${attributionLinks(source.attributions)}` : ''}</figcaption>` : ''
  }
  const googleSource = safeWebUrl(data.google_source_url)
  const sourceCredit = googleSource ? `<p class="al-source-credit">Business information from <a href="${e(googleSource)}" target="_blank" rel="noopener noreferrer">Google Maps</a>${data.google_attributions?.length ? ` · ${attributionLinks(data.google_attributions)}` : ''}. Owner edits may apply.</p>` : ''
  const reviews = data.reviews_verified ? (data.reviews || []).filter(review => review?.text && review?.author).slice(0, 6) : []
  const hours = hoursFor(data)
  const questions = factsQuestions(data)
  const bookingUrl = safeWebUrl(data.cta_url)
  const hasBooking = bookingUrl && !/(^|\.)autolocal\.ai$/i.test(new URL(bookingUrl).hostname)
  const ctaUrl = hasBooking ? bookingUrl : '#contact'
  const ctaText = hasBooking ? data.cta_text || 'Request an appointment' : family === 'summit' ? 'Tell us what you need' : family === 'atelier' ? 'Request an appointment' : family === 'win95' ? 'Start an inquiry' : family === 'myspace' ? 'Send us a message' : family === 'receipt' ? 'Make an inquiry' : 'Start a conversation'
  const serviceHeading = family === 'atelier' ? 'Find your kind of care.' : family === 'ledger' ? 'How we can help.' : family === 'win95' ? 'Services, ready to explore.' : family === 'myspace' ? 'What we’re into.' : family === 'receipt' ? 'The service list.' : 'What can we help with?'
  const contactHeading = family === 'atelier' ? 'Make time<br>for yourself.' : family === 'ledger' ? 'Let’s start<br>a conversation.' : family === 'win95' ? 'Your next step<br>starts here.' : family === 'myspace' ? 'Drop us<br>a message.' : family === 'receipt' ? 'Leave us<br>a note.' : 'Let’s talk about<br>your project.'
  const demo = data.demo || options.mode === 'demo'
  const category = (data.category || '').replace(/[_-]/g, ' ')
  const heroMedia = photo
    ? `<figure class="al-image"><img src="${e(photo)}" alt="${e(ownerPhoto ? data.image_caption || `Photo supplied for ${data.business_name}` : `Google Maps photo of ${data.business_name}`)}" fetchpriority="high" style="object-position:center ${Math.min(100, Math.max(0, data.hero_crop ?? 50))}%">${photoCredit(photo) || (data.image_caption ? `<figcaption>${e(data.image_caption)}</figcaption>` : '')}</figure>`
    : `<div class="al-art" aria-hidden="true"><span class="al-art-dot"></span><span class="al-art-mark">${e(data.business_name.trim().charAt(0))}</span><span class="al-art-caption">${e(location || data.business_name)}</span></div>`
  return `<div class="al-site al-${family}">
  <a href="#main" class="al-skip">Skip to content</a>
  ${demo ? '<div class="al-demo">Fictional business · Template demonstration · No inquiries are sent</div>' : ''}
  <header class="al-header al-shell">${brand(data)}${family === 'receipt' ? '<p class="al-receipt-caption">Services &amp; inquiries</p>' : ''}<nav class="al-nav" aria-label="Main navigation">${services.length ? '<a href="#services">Services</a>' : ''}<a href="#about">Details</a><a href="#contact">Contact</a></nav><a class="al-button" href="${e(ctaUrl)}">${e(ctaText)}<span aria-hidden="true">↗</span></a></header>
  <main id="main">
    <section class="al-hero al-shell" aria-labelledby="hero-title">${family === 'win95' ? '<div class="al-window-title"><span class="al-window-icon" aria-hidden="true"></span><span>Business profile</span></div>' : ''}<div class="al-hero-copy">${family === 'myspace' ? '<p class="al-profile-label">Welcome to our corner of the web</p>' : ''}<p class="al-kicker">${e([category, location].filter(Boolean).join(' · ') || data.business_name)}</p><h1 id="hero-title">${e(data.tagline || data.business_name)}</h1>${data.description ? `<p class="al-intro">${e(data.description)}</p>` : ''}<div class="al-actions"><a class="al-button al-button-accent" href="${e(ctaUrl)}">${e(ctaText)}<span class="al-arrow" aria-hidden="true">→</span></a>${phone ? `<a class="al-text-link" href="${e(phone)}">${e(data.phone)} <span aria-hidden="true">↗</span></a>` : ''}</div></div><div class="al-hero-media">${heroMedia}</div></section>
    ${services.length ? `<div class="al-service-strip"><div class="al-strip-inner al-shell">${services.slice(0, 3).map((service, index) => `<a href="#service-${index + 1}"><span class="al-number">0${index + 1}</span>${e(service.name)}<span class="al-arrow" aria-hidden="true">↗</span></a>`).join('')}</div></div>
    <section id="services" class="al-section al-shell"><div class="al-section-head"><p class="al-kicker">${family === 'ledger' ? 'Our expertise' : 'Our services'}</p><h2>${serviceHeading}</h2></div><div class="al-services">${services.map((service, index) => `<article class="al-service" id="service-${index + 1}"><span class="al-number">${String(index + 1).padStart(2, '0')}</span><h3>${e(service.name)}</h3>${service.description ? `<p>${e(service.description)}</p>` : ''}${service.price ? `<p class="al-price">${e(service.price)}</p>` : ''}<a href="#contact" data-inquiry-service="${e(service.name)}">Ask about this service <span class="al-arrow" aria-hidden="true">→</span></a></article>`).join('')}</div></section>` : ''}
    <section id="about" class="al-about-band"><div class="al-shell al-section al-facts"><div><p class="al-kicker">Good to know</p><h2>${e(data.business_name)}${location ? `<br><span style="opacity:.65">${e(location)}</span>` : ''}</h2>${hours.length ? `<dl class="al-hours">${hours.map(([day, value]) => `<div><dt>${e(day)}</dt><dd>${e(value)}</dd></div>`).join('')}</dl>` : ''}${sourceCredit}</div><dl class="al-fact-list">${data.address && data.show_address !== false ? `<div><dt>Address</dt><dd>${e(data.address)}${location ? `<br>${e(location)}` : ''}</dd></div>` : ''}${data.service_areas?.length ? `<div><dt>Service areas</dt><dd>${data.service_areas.map(e).join(', ')}</dd></div>` : ''}${phone ? `<div><dt>Call</dt><dd><a href="${e(phone)}">${e(data.phone)}</a></dd></div>` : ''}${email ? `<div><dt>Email</dt><dd><a href="mailto:${e(email)}">${e(email)}</a></dd></div>` : ''}<div><dt>Get started</dt><dd><a href="#contact">Tell us what you need <span aria-hidden="true">↗</span></a></dd></div></dl></div></section>
    ${gallery.length ? `<section class="al-shell al-section" aria-labelledby="gallery-title"><div class="al-section-head"><p class="al-kicker">A closer look</p><h2 id="gallery-title">${demo ? 'Illustrative gallery.' : 'Take a look around.'}</h2></div><div class="al-gallery">${gallery.map((url, index) => `<figure><img loading="lazy" src="${e(url)}" alt="${e(demo ? `Illustrative photo ${index + 1}` : `Photo ${index + 1} ${googlePhotoFor(url) ? 'from Google Maps for' : 'supplied for'} ${data.business_name}`)}">${photoCredit(url)}</figure>`).join('')}</div></section>` : ''}
    ${reviews.length ? `<section class="al-shell al-section" aria-labelledby="reviews-title"><div class="al-section-head"><p class="al-kicker">Customer experiences</p><h2 id="reviews-title">In their words.</h2></div><div class="al-review-grid">${reviews.map(review => `<blockquote><p>${e(review.text)}</p><cite>${e(review.author)}${review.date ? ` · ${e(review.date)}` : ''}</cite></blockquote>`).join('')}</div></section>` : ''}
    <section class="al-shell al-section al-faq-grid" aria-labelledby="faq-title"><div><p class="al-kicker">A few details</p><h2 id="faq-title">Before you<br>get in touch.</h2></div><div class="al-faq">${questions.map(question => `<details><summary>${e(question.question)}</summary><p>${e(question.answer)}</p></details>`).join('')}</div></section>
    <section id="contact" class="al-shell al-section al-contact"><div class="al-contact-intro"><p class="al-kicker">Get in touch</p><h2>${contactHeading}</h2><p>Share what you have in mind and how to reach you.</p><div class="al-contact-links">${phone ? `<a class="al-text-link" href="${e(phone)}">${e(data.phone)} <span aria-hidden="true">↗</span></a>` : ''}${email ? `<a class="al-text-link" href="mailto:${e(email)}">${e(email)} <span aria-hidden="true">↗</span></a>` : ''}</div></div>${inquiryForm(data, options)}</section>
  </main><footer class="al-footer"><div class="al-shell al-footer-inner">${family === 'receipt' ? '<div class="al-receipt-end"><div class="al-barcode" aria-hidden="true"></div><p>Thanks for stopping by.</p></div>' : ''}${brand(data)}<nav aria-label="Footer navigation">${services.length ? '<a href="#services">Services</a>' : ''}<a href="#contact">Contact</a></nav><p>© ${new Date().getFullYear()} ${e(data.business_name)}${demo ? ' · Fictional business demo' : ''}<br><a href="https://autolocal.ai" rel="nofollow">Website by AutoLocal</a></p></div></footer>
  <nav class="al-mobile-contact" aria-label="Quick contact">${phone ? `<a class="al-button" href="${e(phone)}">Call</a>` : ''}<a class="al-button ${phone ? 'al-button-accent' : ''}" href="${e(ctaUrl)}">${hasBooking ? 'Book / inquire' : 'Get in touch'}</a></nav>
  </div>`
}
